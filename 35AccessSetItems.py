# Access Items

thisset = {"apple", "banana", "cherry"}

for x in thisset: # prints through all of the values in the set
    print(x)
    
print("banana" in thisset) # checks to see if banana is in the set

# cannot change items in a set but you can add items